# تقرير التنفيذ النهائي — تطبيق «نور»

**الإصدار المسلَّم:** v2.7.0 (build 36)
**تاريخ التقرير:** ١٤ أغسطس ٢٠٢٦

---

## ١) ملخص التنفيذ (المراحل ١ إلى ٩)

### المرحلة ١ — فحص المشروع ✅
فُحص المشروع بالكامل قبل أي تعديل، وحُدِّدت مواضع كل ميزة. أبرز ما كُشف:
- **خصوصية الفيديو غير موجودة إطلاقاً**، وخصوصية الحساب كانت تفلتر الفيد فقط.
- **المتابعة فورية بلا موافقة** (لا يوجد نظام طلبات).
- **لا يوجد نظام رسائل إطلاقاً**.
- **٦٧١ نصاً عربياً مكتوباً داخل الكود** بلا أي نظام ترجمة، رغم وجود قائمة بعشرات اللغات لا تفعل شيئاً.
- **عيب لم يُبلَّغ عنه:** `UserProfileScreen` كان يعرض «لا مقاطع بعد» ثابتة ⇒ **لا أحد يرى مقاطع أحد**.

### المرحلة ٢ — خصوصية الحساب ✅
- الحساب الخاص يعرض **«هذا الحساب خاص»** لغير المخوّلين بدل المقاطع.
- المنع مطبَّق في: الفيد · تبويب المتابَعين · شبكة الملف · **الرابط/المسار المباشر**.
- **إنفاذ مزدوج**: فلترة في العميل + قواعد Firestore.

### المرحلة ٣ — خصوصية الفيديو ✅
- حقل `privacy`: `public` | `friends` | `private`.
- اختيار الخصوصية **عند النشر**، وتغييرها لاحقاً من «مشاركة ← إعدادات الفيديو» (لصاحب المقطع فقط).
- **الصلاحية الفعلية = الأشد تقييداً** بين الحساب والمقطع.
- أُغلقت ثغرة اكتُشفت أثناء التنفيذ: تبويب «متابَعون» كان يتجاوز فحص الخصوصية.

### المرحلة ٤ — اللغات ✅ (تغطية جزئية — انظر §٥)
- **لغتان فقط**: العربية والإنجليزية؛ حُذفت قائمة اللغات القديمة.
- **RTL/LTR تلقائي** عبر `MaterialApp.locale`، وتبديل فوري بلا إعادة تشغيل.
- **حفظ دائم محلياً** (`shared_preferences`) ⇒ يعمل للضيف وقبل تسجيل الدخول، ويُزامَن مع الحساب.

### المرحلة ٥ — التنقّل بين مقاطع المستخدم ✅
- `UserVideosPlayer`: فتح مقطع من ملف شخص يفتح **مقاطعه هو فقط** في `PageView` عمودي.
- شارة سياق «الاسم · n/total» + إشعار نهاية القائمة + الرجوع يعود لصفحته.

### المرحلة ٦ — الخروج بالسحب الأفقي ✅
- سحب أفقي قوي يخرج من مشغّل الملف، **وسهم الرجوع باقٍ**.
- عتبة مزدوجة (مسافة ١١٠px **أو** سرعة ٧٥٠) تمنع الخروج بالخطأ، ولا تتعارض مع التمرير العمودي.

### المرحلة ٧ — إيماءات الرئيسية ✅ (باتجاه صحّحه المستخدم)
| الإيماءة | النتيجة |
|---|---|
| سحب لليسار | تبويب «متابَعون» |
| سحب لليمين | صفحة صاحب الفيديو |
| نقر صورة الناشر | صفحة صاحب الفيديو |
| عمودي | التنقّل المعتاد |

عتبة ٩٠px/٦٥٠ سرعة؛ أزرار الإعجاب والتعليق والمشاركة تعمل دون تأثر.

### المرحلة ٨ — الإعجابات والأصدقاء والملف والرسائل ✅
- **٨٫١** المقاطع المحذوفة تختفي من «الإعجابات» و«المحفوظة» مع تنظيف تلقائي (انظر §٤ للطريقة).
- **٨٫٢** «أصدقاء» = متابعة متبادلة، تظهر وتختفي فوراً؛ ويُبنى عليها «الأصدقاء فقط» في الفيديو والرسائل.
- **٨٫٣** نقر اسم المستخدم ⇒ نسخ فوري · نقر الصورة ⇒ عارض مكبّر بتقريب وإغلاق واضح.
- **٨٫٤** **نظام رسائل كامل** (لم يكن موجوداً): محادثات ثنائية ببثّ حيّ + إعداد «من يمكنه مراسلتي؟» (الكل / الأصدقاء فقط) **مُنفَّذ في قواعد الخادم**.

### المرحلة ٩ — المراجعة النهائية ✅
`flutter analyze` **صفر أخطاء**، وبناء APK ناجح، و`grep` على كلمة «قريب» في `lib/` = **لا نتائج**.

---

## ٢) الملفات المعدّلة والمضافة

### ملفات جديدة
| الملف | الغرض |
|---|---|
| `lib/i18n/app_locale.dart` | نظام اللغة والترجمة (`AppLocale` + `tr()` + جدول ar/en) |
| `lib/models/noor_message.dart` | نموذجا الرسالة والمحادثة |
| `lib/services/message_service.dart` | خدمة الرسائل وإنفاذ سياسة المراسلة |
| `lib/screens/chats_screen.dart` | قائمة المحادثات |
| `lib/screens/chat_screen.dart` | شاشة المحادثة |
| `lib/screens/user_videos_player.dart` | مشغّل مقاطع مستخدم (تنقّل عمودي + خروج أفقي) |
| `lib/services/feed_service.dart` | خوارزمية الترتيب والتوزيع التدريجي |
| `lib/services/block_service.dart` | الحظر وقطع العلاقة |
| `lib/services/player_slot.dart` | بوابة المشغّل الواحد + التحضير المسبق |
| `lib/screens/splash_screen.dart` | شاشة البداية |
| `cloudflare-worker/worker.js` | وسيط رفع الفيديو (يحمي المفتاح السري) |

### ملفات عُدِّلت جوهرياً
| الملف | التعديل |
|---|---|
| `lib/services/follow_service.dart` | طلبات المتابعة · الأصدقاء · `canViewAccount` · منع المتابعة مع الحظر |
| `lib/services/video_service.dart` | `privacy` · `updatePrivacy` · `_pruneDeleted` · الإبلاغ · إشارات الخوارزمية |
| `lib/services/notif_service.dart` | معرّفات ثابتة تمنع تكرار الإشعارات + `remove()` عند التراجع |
| `lib/screens/user_profile_screen.dart` | شبكة المقاطع · بوابتا الحظر والخصوصية · زر ٤ حالات · نسخ اليوزر · عارض الصورة · زر المراسلة |
| `lib/screens/inbox_screen.dart` | تبويب طلبات الصداقة بعدّاد · تجميع زمني · مدخل الرسائل |
| `lib/screens/settings_screen.dart` | إعدادات حقيقية محفوظة · اللغة · سياسة الرسائل · إزالة البيانات الوهمية |
| `lib/widgets/clip_view.dart` | media_kit · إيماءات · خصوصية · إشارات · حذف |
| `lib/screens/feed_screen.dart` | تحميل لمرة واحدة · سحب للتحديث · تحميل تدريجي |
| `lib/screens/publish_screen.dart` | منتقي الخصوصية عند النشر |
| `lib/screens/discover_screen.dart` | سهم رجوع مشروط · حالة متابعة حيّة موحّدة |
| `firestore.rules` | إنفاذ خادمي لكل ما سبق |

---

## ٣) الاختبارات المنفّذة ونتائجها

**ما نفّذتُه أنا (آلي):**
| الاختبار | النتيجة |
|---|---|
| `flutter analyze` بعد كل دفعة | ✅ صفر أخطاء |
| بناء APK لكل دفعة (٧ دفعات) | ✅ نجح |
| `grep` على «قريب» في `lib/` | ✅ لا نتائج |
| تدقيق سهم الرجوع في كل شاشة مدفوعة | ✅ جميعها سليمة (أُصلحت «استكشف») |
| اختبار حيّ لوسيط Cloudflare | ✅ رجع `200` مع رابط رفع صالح |

**ما اختبره المستخدم على الجهاز وأكّده:**
| السيناريو | النتيجة |
|---|---|
| تشغيل الفيديو بعد تبديل المحرّك | ✅ يعمل |
| رفع ونشر مقطع | ✅ يعمل |
| ظهور «هذا الحساب خاص» | ✅ يعمل |
| طلب المتابعة | ✅ بعد إصلاح قاعدة القراءة |

**⚠️ لم تُختبر ميدانياً بعد** (تحتاج حسابين واختبار يدوي): الرسائل وسياسة «الأصدقاء فقط» · خصوصية الفيديو بحالاتها الثلاث × المشاهدين الأربعة · قبول/رفض الطلبات · اختفاء المقطع المحذوف من الإعجابات · إيماءات الرئيسية · تبديل اللغة والاتجاه.

> **لا أعتبر هذه البنود مكتملة قبل تأكيدك الميداني.**

---

## ٤) القرارات والافتراضات التقنية

1. **«صديق» = متابعة متبادلة** تُحسب من `follows` بوجود الاتجاهين — بلا مجموعة جديدة ولا تكرار بيانات.

2. **اتجاه الإيماءات في RTL:** الاتجاه يُقاس بالإحداثيات الفعلية للإصبع لا بالاتجاه المنطقي، فيبقى السلوك ثابتاً ولا ينقلب مع اللغة. وقد **عُكس عن المستند بطلبك الصريح**: يسار ⇒ متابَعون، يمين ⇒ صفحة الشخص.

3. **٨٫١ — حذف المقطع من إعجابات الآخرين:** العميل **لا يملك صلاحية حذف بيانات مستخدمين آخرين**، والحل الخادمي يتطلب Cloud Functions (خطة Blaze غير متاحة). **المطبَّق:** المقاطع المحذوفة تُفلتر عند القراءة ويُنظَّف سجل المستخدم نفسه تلقائياً. **النتيجة الظاهرة مطابقة تماماً**، لكن الحذف ليس فيزيائياً من بيانات الآخرين.

4. **الإنفاذ الخادمي للرسائل:** قاعدة `conversations` تتحقق من المتابعة المتبادلة عبر `exists()` على وثيقتَي `follows` — فالمنع حقيقي لا واجهي.

5. **تكرار الإشعارات:** الأنواع القابلة للتكرار (متابعة/طلب/إعجاب) لها **معرّف ثابت** فيستبدل الجديد القديم؛ والتراجع يحذف الإشعار. التعليقات تبقى منفصلة لأن كل تعليق حدث مستقل.

6. **الفيد يُحمَّل مرة واحدة** ولا يُبثّ حيّاً — هذا ما أنهى تعليق التنقّل، لأن أي إعجاب كان يعيد بناء القائمة أثناء التمرير.

7. **مشغّل واحد نشط + محضَّر واحد بحد أقصى**، مع مراقب ٢٫٥ ثانية يتخلّى عن المحضَّر إن لم تُرسم أول لقطة (يمنع الشاشة السوداء).

8. **استضافة الفيديو:** Cloudflare Stream (تحويل تلقائي إلى HLS) عبر وسيط Worker — التطبيق **لا يحمل أي مفتاح سري**.

---

## ٥) المشكلات المتبقية (بصراحة — العمل غير مكتمل بوجودها)

1. **الترجمة جزئية.** النصوص الرئيسية مترجمة، لكن تبقى نصوص ثانوية بالعربية: بعض رسائل التنبيه التفصيلية · نص الشروط وسياسة الخصوصية · بعض العناوين الفرعية. **الإكمال متدرّج**: كل نص يُضاف كمفتاح إلى `lib/i18n/app_locale.dart`.

2. **قواعد الأمان تحتاج نشراً يدوياً** بعد كل تحديث يمسّها. إن لم تُنشر، تفشل الميزات **بصمت** — وقد حدث ذلك مرتين خلال العمل.

3. **بنود لم تُختبر ميدانياً** (القائمة في §٣).

4. **مقاطع Cloudinary القديمة** تبقى أبطأ من الجديدة (ملف واحد مقابل بثّ مقسّم). يُنصح بحذفها وإعادة رفعها.

5. **البحث لا يشمل الفيديوهات** — «استكشف» يبحث في المستخدمين فقط. لم يُطلب صراحةً لكنه فجوة واضحة.

6. **`AppState` القديم** ما زال موجوداً في المشروع وإن أُزيل استخدامه من الإعدادات — يُنظَّف لاحقاً.

7. **حذف الحساب** يحذف المصادقة ووثيقة المستخدم، لكن **لا يحذف مقاطعه وتعليقاته** (يحتاج تنظيفاً خادمياً).
